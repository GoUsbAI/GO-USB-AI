# @go-usb-ai/remote

Remote access runtime for GoUsbAi.

This package contains the device registration flow, websocket relay connector,
remote request bridge, service-mode runtime module, and the CLI command facade
used by `go-usb-ai`.
