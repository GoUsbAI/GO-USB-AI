# @go-usb-ai/go-usb-ai-ncp-runtime-http-client

Pure runtime package that bridges a remote HTTP/SSE NCP agent endpoint into the `NcpAgentRuntime` contract used by GoUsbAi.

It is intentionally generic:

- no Hermes-specific logic
- no runtime type registration logic
- no UI config concerns
